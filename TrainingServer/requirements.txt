# =====================================================
# MediBridge TrainingServer 의존성 (Python 3.12 + Ubuntu 24.04 + GPU)
# =====================================================
# 학습용 — 추론 서버보다 라이브러리 무거움.
# =====================================================

# ----- PyTorch (CUDA 빌드) -----
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0
torchvision>=0.19.0

# ----- YOLO 학습 -----
ultralytics>=8.3.0

# ----- PaddleOCR fine-tuning -----
paddlepaddle-gpu>=2.6.0
paddleocr>=2.8.0

# ----- 데이터 처리 -----
opencv-python>=4.10.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
pillow>=10.4.0

# ----- 학습 모니터링 -----
tensorboard>=2.18.0
# wandb>=0.18.0    # 선택 (Weights & Biases)
# mlflow>=2.16.0   # 선택

# ----- Jupyter (실험·EDA) -----
jupyterlab>=4.2.0
ipykernel>=6.29.0

# ----- 도구 -----
loguru>=0.7.2
tqdm>=4.66.0
pyyaml>=6.0.0
