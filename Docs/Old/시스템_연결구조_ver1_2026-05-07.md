# 메디브릿지 시스템 연결 구조 (v1.0 초안)

| 항목 | 내용 |
| --- | --- |
| **문서 종류** | 시스템 연결 구조 정의 (역할 분류 + 디바이스 간 연결 흐름) |
| **버전** | v1.0 (초안 — 사용자 검토 중) |
| **작성일** | 2026-05-07 |
| **상태** | 🟡 **WIP — 사용자가 단계별 흐름을 추가하며 검토 진행 중** |

> ⚠️ **본 문서는 기존 v2/v3 문서(시스템 흐름 정리본·기획서 등)의 수정 제안임**.
> 핵심 차이: **데이터 보관 PC** 신규 도입 — 기존 4대 구성(GUI/메인/추론/학습) → **5대 + 폰**.
> 검토 완료 후 확정 시 기존 v2 문서들을 v3 로 일괄 갱신해야 함.

---

## 1. 디바이스 / 서버 역할 분류

| # | 디바이스 | OS / 하드웨어 | 핵심 역할 (한 줄) |
| --- | --- | --- | --- |
| ① | **핸드폰 (안드로이드 S24)** | Android 14+ | 카메라·마이크·온디바이스 STT 자원 제공 (입력 디바이스) |
| ② | **클라이언트 PC** | Windows 10/11 | 사용자 GUI + 폰 조작 + 메인서버 통신 + 화면 캡쳐 |
| ③ | **메인 서버 PC** | Ubuntu 24.04 | 비즈니스 로직 + MariaDB(메타데이터) + **라우팅 컨트롤 타워** |
| ④ | **데이터 보관 PC** ⭐ 신규 | Ubuntu 24.04 (대용량 디스크) | 사진·음성·기타 대용량 바이너리 **영속 저장** |
| ⑤ | **추론 PC** | Ubuntu 24.04 (GPU) | YOLO·PaddleOCR·OpenCV·LLM 추론 |
| ⑥ | **학습 서버** | Ubuntu 24.04 (GPU) | 모델 학습 (운용과 격리) |

### 역할별 핵심 보유 자원

| 디바이스 | DB | GPU | 디스크 (대용량) | 카메라/마이크 |
| --- | --- | --- | --- | --- |
| ① 핸드폰 | — | (NPU/Galaxy AI) | — | ✓ |
| ② 클라이언트 PC | — | — | — | — |
| ③ 메인 서버 | **MariaDB (메타)** | — | (보통) | — |
| ④ **데이터 보관 PC** | — | — | **✓ 대용량 ✓** | — |
| ⑤ 추론 PC | — | ✓ | (모델 가중치만) | — |
| ⑥ 학습 서버 | — | ✓ | ✓ (학습 데이터셋) | — |

---

## 2. 디바이스 간 연결 1:1 (사용자 정의 흐름)

### 2.1 ① 핸드폰 ↔ ② 클라이언트 PC (USB)

| 동작 | 흐름 |
| --- | --- |
| **촬영 조작** | 클라 PC → 폰 (ADB/scrcpy) → 폰 카메라 앱 제어 |
| **촬영 화면 보기** | 폰 → 클라 PC (scrcpy 미러링) |
| **사진 데이터 확보** | 클라 PC 가 폰 화면 캡쳐 (`adb exec-out screencap -p`) → PNG 직접 수신 |
| **음성 인식 시작** | 클라 PC → 폰 (음성 인식 ON 신호) |
| **음성 인식 결과** | 폰 내부 STT → 텍스트 변환 → 클라 PC 로 텍스트만 송신 (음성 바이너리 X) |
| **음성 안내** | 메인서버 → 클라 PC → 폰 TTS (또는 클라 PC 자체 TTS) |
| **사용 시점 (음성)** | 약품 등록 시 — STT 결과를 클라 PC 경유해 LLM 보정 |

> 📌 **음성 흐름 핵심**: 음성 바이너리는 폰 외부로 나가지 않음. 텍스트만 클라 PC → LLM(메인/추론서버) → 보정된 텍스트 응답.

### 2.2 ② 클라이언트 PC ↔ ③ 메인 서버 (LAN)

| 동작 | 흐름 |
| --- | --- |
| **사진 업로드 신호** | 클라 PC → 메인 서버 ("사진 업로드 의향" 통지 + 메타데이터: 크기·MIME·요청자) |
| **저장 가능 판정** | 메인 서버 → 데이터 보관 PC ("디스크·메모리 여유 있나?") |
| **저장 경로 발급** | 데이터 보관 PC → 메인 서버 (`OK + 경로`) |
| **클라에게 응답** | 메인 서버 → 클라 PC (`OK + 데이터 보관 PC 경로`) |
| **메타데이터 저장** | 메인 서버 → MariaDB (`경로 + 메타데이터` 한 행 INSERT) |
| **실제 사진 전송** | 클라 PC → **데이터 보관 PC 직접** (메인서버 우회, 발급된 경로로 PUT) |

> 📌 **컨트롤 평면(메인) ↔ 데이터 평면(데이터 보관) 분리**. AWS S3+RDS 패턴과 유사.

### 2.3 ③ 메인 서버 ↔ ④ 데이터 보관 PC (사진 데이터 평면)

| 동작 | 흐름 |
| --- | --- |
| **사전 점검** | 메인 서버 → 데이터 보관 PC (디스크·메모리 상태 질의) |
| **경로 사전 발급** | 메인 서버 ← 데이터 보관 PC (저장 가능 경로 응답) |
| **사진 본체 저장** | 클라/추론/학습 PC → 데이터 보관 PC (직접 PUT, 경로는 메인서버에서 받음) |
| **사진 본체 조회** | 클라/추론/학습 PC → 메인서버 (경로 질의) → 데이터 보관 PC (직접 GET) |
| **메타데이터 일관성** | 메인 서버 MariaDB 의 경로 ↔ 데이터 보관 PC 실제 파일 1:1 매칭 |

### 2.4 추론·학습 PC 의 사진 접근 (사용자 명시)

> "이후 이 사진데이터는 추론PC나 학습PC에서도 불러올 수 있도록 할 것, 이 때도 경로는 메인서버를 통해 물어보고 실제 데이터는 데이터보관PC로부터 받을 것"

| 디바이스 | 흐름 |
| --- | --- |
| ⑤ 추론 PC | 1. 메인서버에 "request_id 사진 경로?" 질의 → 2. 메인서버가 MariaDB 조회 → 경로 응답 → 3. 추론 PC 가 데이터 보관 PC 에서 직접 GET → 4. YOLO·OCR 추론 |
| ⑥ 학습 서버 | (학습 시점) 메인서버에 학습용 데이터셋 경로 일괄 질의 → 데이터 보관 PC 에서 SCP/일괄 다운로드 |

---

## 3. 미정의 / 추후 추가 흐름 (사용자 검토 진행 중)

다음 흐름들은 본 v1.0 에서 **자리만 정의**, 사용자 단계별 추가 예정:

- 🔲 **③ 메인 서버 ↔ ⑤ 추론 PC** — 추론 요청 흐름 (현재 REST API 정의는 v2 에 있음)
- 🔲 **③ 메인 서버 ↔ 식약처 OpenAPI** — e약은요 lazy 캐싱
- 🔲 **⑥ 학습 서버 → ⑤ 추론 PC** — 모델 배포 (SCP)
- 🔲 **클라 PC ↔ 메인 서버 (사진 외)** — 회원가입·로그인·복약이력·보고서 등 일반 API
- 🔲 **음성 LLM 보정 흐름** — 클라 PC 의 음성 텍스트가 LLM 으로 가는 경로 (메인서버? 추론서버 직통?)
- 🔲 **DUR 위험 안내 흐름** — 식별 후 메인서버의 DurChecker 호출
- 🔲 **데이터 보관 PC 보안 정책** — 인증 토큰? IP allowlist? 메인서버 발급 서명 URL?

---

## 4. 데이터 흐름 시각화 (정의된 부분만)

### 4.1 사진 업로드 — 컨트롤 평면 + 데이터 평면 분리

```
[① 핸드폰 카메라]
   ↑↓ (USB ADB)
   │ scrcpy 미러링 + screencap 캡쳐
   ↓
[② 클라 PC]
   │
   │ (1) "사진 업로드 의향" + 메타데이터
   ├──────────────────────→ [③ 메인 서버]
   │                              │
   │                              │ (2) "여유 있나?" + 경로 질의
   │                              ├──────→ [④ 데이터 보관 PC]
   │                              │              │
   │                              │ (3) OK +    │
   │                              │     경로     │
   │                              ←──────────────┤
   │                              │
   │                              │ (4) MariaDB INSERT (메타 + 경로)
   │                              │
   │ (5) "OK + 데이터 보관 PC 경로"
   ←──────────────────────┤
   │
   │ (6) PUT 사진 바이너리 (직접)
   └──────────────────────────────────────────→ [④ 데이터 보관 PC]
```

### 4.2 추론 시 사진 접근

```
[⑤ 추론 PC]
   │
   │ (1) "request_id 의 사진 경로?"
   ├──────────────────────→ [③ 메인 서버]
   │                              │
   │                              │ MariaDB SELECT 경로
   │                              │
   │ (2) 경로 응답
   ←──────────────────────┤
   │
   │ (3) GET 사진 바이너리 (직접)
   └──────────────────────────────────────────→ [④ 데이터 보관 PC]
   │
   │ (4) 추론 처리 (YOLO·OCR)
   ↓
[추론 결과 → 메인 서버 → 클라 PC]
```

---

## 5. 본 v1.0 vs 기존 v2/v3 차이

| 항목 | 기존 v2/v3 | 본 v1.0 (제안) |
| --- | --- | --- |
| 디바이스 수 | 4대 + 폰 (GUI/메인/추론/학습 + S24) | **5대 + 폰** (GUI/메인/**데이터 보관**/추론/학습 + S24) |
| 사진 저장 위치 | 메인 서버 (`uploads/` 폴더) | **데이터 보관 PC** (전용 디바이스) |
| 메인 서버 부담 | 비즈니스 로직 + 파일 I/O + DB | 비즈니스 로직 + DB 메타만 |
| 클라 → 메인 흐름 | 사진 직접 POST (10MB) | 신호만 → 경로 받음 → 데이터 보관 PC 로 PUT |
| 추론 PC 사진 접근 | 메인서버 경유로 받음 | 메인서버에 경로 질의 → 데이터 보관 PC 에서 직접 GET |
| 디스크 사양 부담 분산 | 메인서버에 집중 | 데이터 보관 PC 에 집중 (메인은 가벼워짐) |

### 장점 (제안 구조)
- **부하 분산**: 메인 서버는 메타만 — DB·라우팅에 집중. 사진 I/O 부담 X
- **확장성**: 데이터 보관 PC 만 추가/교체로 저장 용량 확장
- **추론·학습 효율**: 추론·학습 PC 가 메인 서버를 거치지 않고 직접 데이터 접근
- **백업·복제**: 데이터 보관 PC 단독으로 백업·복제 정책 적용 가능

### 우려/검토 필요
- **PC 1대 추가**: 환경 구성 부담 ↑ (16일 일정 영향)
- **단일 장애점**: 데이터 보관 PC 다운 시 사진 접근 불가 (백업 정책 필요)
- **보안**: 데이터 보관 PC 직접 접근 통제 (메인서버 발급 서명 URL? 토큰?)
- **메타-실제 일관성**: MariaDB 행과 실제 파일 동기화 (트랜잭션 어려움)
- **임시 저장**: 메인 서버가 데이터 보관 PC 와 통신 실패 시 fallback (메인서버 임시 디스크?)

---

## 6. 변경 이력

| 버전 | 일자 | 작성자 | 변경 사항 |
| --- | --- | --- | --- |
| v1.0 | 2026-05-07 | 팀 (3인) | 초안 — 사용자 제안 5대 + 폰 구조 정리. ① 핸드폰 ↔ ② 클라 PC 흐름 정의, ② 클라 PC ↔ ③ 메인 서버 흐름 정의, ③ 메인 ↔ ④ 데이터 보관 PC 흐름 정의. ⑤ 추론·⑥ 학습은 자리만. 본 문서는 기존 v2/v3 의 4대 구성과 다름 — 검토 후 v3 로 통합 결정 필요 |

---

## 7. 다음 단계 (사용자 검토 흐름)

사용자가 다음 흐름들을 단계별로 추가하면, 본 문서에 이어 붙이고 v1.1, v1.2 로 갱신 진행:

1. ⑤ 추론 PC ↔ ③ 메인 서버 (추론 요청)
2. ⑥ 학습 서버 ↔ ④ 데이터 보관 PC (학습 데이터 일괄)
3. 음성 LLM 보정 흐름
4. DUR 검출 흐름
5. 보안·인증 정책 (데이터 보관 PC 접근 토큰 등)

검토 완료 후 본 v1.x 의 내용을:
- 「시스템 흐름 정리본 v3」으로 통합
- 「기획서 v3」/「프로토콜 v3」/「DB ERD v4」 로 파급 갱신
